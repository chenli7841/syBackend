@model WebUI.Models.OrderInventoryResponse

@{
    ViewData["Title"] = "优惠券";
}

@section Styles
{
    <style>
        div.card .order-card-body, #order-details {
            padding-top: 1rem;
            padding-bottom: 1rem;
        }

        #order-listing_wrapper div#order-listing_filter {
            display: none;
        }

            #order-listing_wrapper div#order-listing_filter input {
                width: 600px;
            }
    </style>
}

<div class="row">
    <div class="col-md-12 grid-margin">
        <div class="card">
            <div class="card-body order-card-body">
                <div class="table-responsive">
                    <table id='otherorder-listing-search' class="table table-striped">
                        <thead>
                            <tr>
                                <th>批次名</th>
                                <th>优惠券数量</th>
                                <th>详情</th>
                                <th>导出</th>
                                <th>刪除</th>
                            </tr>
                        </thead>
                    </table>
                </div>
            </div>
        </div>
    </div>
</div>

@section Scripts
{
    <script>
        $(document).ready(() => {
            const tableId = 'otherorder-listing-search';
            $(`#${tableId} thead tr`).clone(true).appendTo(`#${tableId} thead`);
            $(`#${tableId} thead tr:eq(1) th`).each(function(i) {
                if (!$(this).data('searchable')) {
                    $(this).html('');
                    return;
                }

                const title = $(this).text();
                $(this).css('padding-left', '0');
                $(this).html('<input type="text" class="form-control form-control-sm" placeholder="搜索 ' + title + '" />');

                $('input', this).on('keyup change',
                    function(event) {
                        if (event.key !== 'Enter') {
                            return;
                        }

                        if (orderTable.column(i).search() !== this.value) {
                            orderTable.column(i).search(this.value).draw();
                        }
                    });
            });

            var dataTableOptions = {
                "processing": true, // for show progress bar
                "serverSide": true, // for process server side
                "filter": true, // this is for disable filter (search box)
                "orderMulti": false, // for disable multiple column at once
                "ordering": false,
                "stateSave": false,
                "lengthChange": false, // for show entries dropdown
                "ajax": {
                    "url": '@Url.Action("LoadCouponBatch")',
                    "type": "POST",
                    "datatype": "json"
                },
                "columns": [
                    { "data": "name", "autoWidth": true },
                    { "data": "numberOfCoupons", "autoWidth": true },
                    {
                        "render": (data, type, full, meta) => `<a href='@Url.Action("EditCoupon")?id=${full.id
                            }'><i class="fa fa-edit fa-lg"></i></a>`
                    },
                    {
                        "render": (data, type, full, meta) => `<a href='@Url.Action("ExportCouponBatch")?id=${full.id
                            }' target='_blank'><i class="fa fa-table fa-lg"></i></a>`
                    },
                    {
                        "render": (data, type, full, meta) => `<a href='@Url.Action("DeleteCouponBatch")?id=${full.id
                            }'><i class="fa fa-trash-o fa-lg" style="color: red"></i></a>`
                    }
                ],
                "language": {
                    // https://datatables.net/manual/i18n
                    "processing": "处理中...",
                    search: "_INPUT_",
                    searchPlaceholder: "搜索...",
                    "info": "显示 _TOTAL_ 里面的_START_ 到 _END_",
                    "paginate": {
                        "previous": "前一页",
                        "next": "下一页"
                    },
                },
            };

            const orderTable = $(`#${tableId}`).DataTable(dataTableOptions);
            $("input[type='search']").closest('div.row').hide();
        });
    </script>
}